const { riskAlerts } = require('../../mock/data')
const { complianceText } = require('../../utils/common')
Page({ data:{ riskAlerts, complianceText, complianceTips:'请核对说明书 / 建议咨询药师或医生 / 请勿自行增减药量' } })
